<?php

/**
 * @file
 * Contains idc_product.module.
 */

use Drupal\Core\Routing\RouteMatchInterface;

/**
 * Implements hook_help().
 */
function idc_product_help($route_name, RouteMatchInterface $route_match) {
  switch ($route_name) {
    // Main module help for the idc_product module.
    case 'help.page.idc_product':
      $output = '';
      $output .= '<h3>' . t('About') . '</h3>';
      $output .= '<p>' . t('The idc products.') . '</p>';
      return $output;

    default:
  }
}

/**
 * Implements hook_theme().
 */
function idc_product_theme() {
  $theme = [];
  $theme['idc_product'] = [
    'render element' => 'elements',
    'file' => 'idc_product.page.inc',
    'template' => 'idc_product',
  ];
  $theme['idc_product_content_add_list'] = [
    'render element' => 'content',
    'variables' => ['content' => NULL],
    'file' => 'idc_product.page.inc',
  ];
  return $theme;
}

/**
* Implements hook_theme_suggestions_HOOK().
*/
function idc_product_theme_suggestions_idc_product(array $variables) {
  $suggestions = [];
  $entity = $variables['elements']['#idc_product'];
  $sanitized_view_mode = strtr($variables['elements']['#view_mode'], '.', '_');

  $suggestions[] = 'idc_product__' . $sanitized_view_mode;
  $suggestions[] = 'idc_product__' . $entity->bundle();
  $suggestions[] = 'idc_product__' . $entity->bundle() . '__' . $sanitized_view_mode;
  $suggestions[] = 'idc_product__' . $entity->id();
  $suggestions[] = 'idc_product__' . $entity->id() . '__' . $sanitized_view_mode;
  return $suggestions;
}
